<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>{{css}}</style>
</head>
<body>
  <header>
    {{#with about}}
      <h1>{{name}}</h1>
    {{/with}}
  </header>
  <main>
    {{#each main}}
      {{> section this}}
    {{/each}}
  </main>
  {{!-- <aside>
    {{#each aside}}
    {{/each}}
  </aside> --}}
  {{!-- <footer>
    <ul>
      {{#each references}}
        <li>{{title}}, {{description}}</li>
      {{/each}}
    </ul>
  </footer> --}}
</body>
</html>
