<header>
  <h1>{{name}}</h1>
  <h2>{{position}}</h2>
  {{#each content}}
    <h3>
      {{#if type}}
        {{type}}:
      {{/if}}
      {{#if url}}<a href="{{url}}">{{/if}}
        {{name}}
      {{#if url}}</a>{{/if}}
    </h3>
  {{/each}}
</header>