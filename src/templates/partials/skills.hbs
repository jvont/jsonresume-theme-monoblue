{{#ifwith resume.skills}}
	<section>
		{{> section-header name="Skills"}}
		<ul id="skills-list">
			{{#each this}}
				<li>
					<header>
						<h3>{{name}}</h3>
					</header>
					{{#if level}}
						{{level}}
					{{/if}}
					{{#ifwith keywords}}
						<ul>
							{{#each this}}
								<li>{{.}}</li>
							{{/each}}
						</ul>
					{{/ifwith}}
				</li>
			{{/each}}
		</ul>
	</section>
{{/ifwith}}