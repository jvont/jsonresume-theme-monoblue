{{#ifwith resume.references}}
	<section>
		<header>
			<h2>References</h2>
		</header>
		<ul id="references-list">
			{{#each this}}
				<li>
					<header>
						<h3>{{name}}</h3>
					</header>
					{{#if reference}}
						<p>{{reference}}</p>
					{{/if}}
				</li>
			{{/each}}
		</ul>
	</section>
{{/ifwith}}